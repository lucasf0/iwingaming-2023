import React from "react";

function SearchBox() {
  return (
    <input
      type="text"
      onChange={(e) => {
        
      }}
      className="searchbox w-100"
      placeholder="search"
    />
  );
}

export default SearchBox;
